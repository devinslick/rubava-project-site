<p id="notice"><%= notice %></p>

<h1><%= link_to @project.name, @project.prodURL %></h1>
<p class="centered"><%= @project.status %></p>
<div>
  <p>
    <%= @project.description %>
  </p>
</div>

<div>
  <p>
    <b>License:</b>
    <%= @project.license %>
  </p>
  <p>
    <% if !@project.repo.empty? %>
    <b>Repo:</b>
    <%= @project.repo %>
    <% else %>
    <i><b>no repo yet</b></i>
    <% end -%>
  </p>
</div>

<div>
  <%= link_to 'Edit', edit_project_path(@project) %> |
  <%= link_to 'Back', projects_path %>
</div>